name: family_work_ledger
description: 简洁账本风的家庭/工作记账应用 (Flutter + SQLite + Lighthouse API)

publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.3
  path_provider: ^2.1.2
  intl: ^0.19.0
  csv: ^6.0.0
  http: ^1.2.2
  shared_preferences: ^2.3.2
  speech_to_text: ^6.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
